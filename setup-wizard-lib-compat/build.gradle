apply plugin: 'com.android.library'

android {
    compileSdkVersion COMPILE_SDK_VERSION
    defaultConfig {
        minSdkVersion setupwizardlibcompat.MIN_SDK_VERSION
    }
    // TODO replace with https://issuetracker.google.com/issues/72050365 once released.
    libraryVariants.all {
        it.generateBuildConfig.enabled = false
    }
    sourceSets {
        main {
            manifest.srcFile rootProject.file('main/AndroidManifest.xml')

            java.srcDirs += rootProject.file('main/src')
            res.srcDirs += rootProject.file('main/res')

            java.srcDirs += rootProject.file('gingerbread/src')
            res.srcDirs += rootProject.file('gingerbread/res')

            java.srcDirs += rootProject.file('recyclerview/src')
            res.srcDirs += rootProject.file('recyclerview/res')
        }
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    implementation 'com.android.support:appcompat-v7:' + ANDROID_SUPPORT_LIBRARY_VERSION
    implementation 'com.android.support:support-annotations:' + ANDROID_SUPPORT_LIBRARY_VERSION
    implementation 'com.android.support:support-compat:' + ANDROID_SUPPORT_LIBRARY_VERSION
    implementation 'com.android.support:support-core-ui:' + ANDROID_SUPPORT_LIBRARY_VERSION
    implementation 'com.android.support:recyclerview-v7:' + ANDROID_SUPPORT_LIBRARY_VERSION
}

apply from: rootProject.file('bintray.gradle')